{
  "name": "floscleaning",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
	"deploy-gh-pages":"npx gh-pages -d client/build -b gh-pages -f",
	"build-and-deploy": "cd client && npm run build && cd ../ && npm run deploy-gh-pages"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "create-react-app": "^1.2.1",
    "gh-pages": "^5.0.0"
  }
}
